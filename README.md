# How to run

```
# build jar file
./gradlew shadowJar

# run with `java -jar build/libs/BlueBenchmarks-0.1-all.jar BENCHMARKCLASS`
java -jar build/libs/BlueBenchmarks-0.1-all.jar blue.SimpleBenchmark
```